/! NAVBAR
= render partial: "style_guide/v1/components/navbar"
/! SEARCHBAR
= render partial: "style_guide/v1/components/searchbar"
script src="https://cdnjs.cloudflare.com/ajax/libs/rome/2.1.22/rome.js"

javascript:
  | rome(dt, { time: false });
  | rome(dt2, { time: false });
  | $(".flex-form").submit(function(){    $(".submitBtn").attr("disabled", true);                               return true;
  | });
/! PRODUCTS BLOC
= render partial: "style_guide/v1/components/products_bloc"
/! FOOTER
= render partial: "style_guide/v1/components/footer"
